const Answer = (props) => {
    
        return(
            <div>
                <h1>Answer</h1>
                <button onClick={props.handleLogout}>Logout</button>
            </div>
        )
    }

export default Answer